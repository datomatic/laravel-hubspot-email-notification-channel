name: PHP Linting

on: [push]

jobs:
  phplint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: "laravel-pint"
        uses: aglipanci/laravel-pint-action@2.4
        with:
          verboseMode: true
          testMode: true
